import React, { useEffect, useState, useCallback } from "react";

export interface MenuState {
  routePath: string;
}

const Info: React.FC = () => {
  return <div>간단한 자기 소개</div>;
};

export default Info;
